---
'@red-hat-developer-hub/backstage-plugin-bulk-import-backend': patch
---

When a GitHub App is configured but no installation token can be issued, orchestrator import errors now list the accounts the App is already installed on.
